# Copy to .env for browser CV lab (npm run dev)
# Do NOT use on Capacitor builds — native always uses MobileApp (production UI).

VITE_VISION_LOCAL=true
VITE_WEB_DEBUG=true
VITE_DETECTION_MODE=stable_v1

# Optional: legacy alias (same as VITE_WEB_DEBUG)
# VITE_DEV_MODE=true
