/** @see shared/config — re-exports for backward-compatible imports. */
export {
  WEB_DEBUG,
  MOBILE_PRODUCTION,
  DEV_MODE,
  IS_NATIVE,
  API_BASE,
  VISION_LOCAL,
  USE_REMOTE_VISION,
  OFFLINE_MODE,
  getDetectionMode,
  detectionModeLabel,
  DETECTION_MODES,
  type DetectionMode,
} from "@/shared/config";
