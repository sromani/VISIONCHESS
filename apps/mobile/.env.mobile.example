# Copy to .env for iOS/Android builds (cap sync) — production UI only

VITE_VISION_LOCAL=true
VITE_DETECTION_MODE=stable_v1

# Do NOT set VITE_WEB_DEBUG on native — ignored on device anyway (IS_NATIVE forces MobileApp)
