# Phase 2: local YOLO → FEN → interactive board (100% offline)
VITE_VISION_LOCAL=true

# Board localization mode (default: stable_v1 — simple, consistent)
# VITE_DETECTION_MODE=stable_v1
# VITE_DETECTION_MODE=experimental
# VITE_DETECTION_MODE=yolo_v1
# VITE_DETECTION_MODE=mesh_v2

# Web CV lab only (browser) — side panel, benchmarks, debug overlays
# cp .env.web.example .env
# VITE_WEB_DEBUG=true

# Legacy alias for VITE_WEB_DEBUG:
# VITE_DEV_MODE=true

# Remote API (only when VITE_VISION_LOCAL is not true)
# VITE_API_URL=http://192.168.1.10:8001/api/v1

# VITE_DEV_MODE=true
